$(document).ready(() => {
    $("#form-one").submit((event) => {
        event.preventDefault();
        const password = $("#word").val();
    });
});
if (password.charAt(0).toUpperCase() !== password.charAt(0)){
    return "uppercase joor"
}
$("#display").text(result)